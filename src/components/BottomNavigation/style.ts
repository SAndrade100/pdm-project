import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    navigation: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        color: 'white',
        marginTop: 16,
        paddingVertical: 10,
        borderTopWidth: 1,
        borderTopColor: '#ccc',
    },
    navButton: {
        padding: 10,
    },
});